@model OnlineStoreDomain.Model.Order

@{
    ViewData["Title"] = "Видалити замовлення";
}

<div class="container mt-4 p-4 bg-light shadow rounded">
    <h1 class="mb-4 text-black">Видалити замовлення #@Model.Id</h1>

    <h3>Ви впевнені, що хочете видалити це замовлення?</h3>
    <div>
        <dl class="row">
            <dt class="col-sm-2">Ім’я</dt>
            <dd class="col-sm-10">@Model.Name</dd>
            <dt class="col-sm-2">Прізвище</dt>
            <dd class="col-sm-10">@Model.LastName</dd>
            <dt class="col-sm-2">Email</dt>
            <dd class="col-sm-10">@Model.Email</dd>
            <dt class="col-sm-2">Телефон</dt>
            <dd class="col-sm-10">@Model.Phone</dd>
            <dt class="col-sm-2">Служба доставки</dt>
            <dd class="col-sm-10">@Model.DeliveryService.Name</dd>
            <dt class="col-sm-2">Відділення</dt>
            <dd class="col-sm-10">@Model.DeliveryDepartment.Name</dd>
            <dt class="col-sm-2">Статус</dt>
            <dd class="col-sm-10">@Model.StatusType.Name</dd>
            <dt class="col-sm-2">Сума</dt>
            <dd class="col-sm-10">@Model.OrderPrice.ToString("C", new System.Globalization.CultureInfo("uk-UA"))</dd>
            <dt class="col-sm-2">Дата реєстрації</dt>
            <dd class="col-sm-10">@Model.RegistrationDate.ToString("dd.MM.yyyy HH:mm")</dd>
            <dt class="col-sm-2">Дата доставки</dt>
            <dd class="col-sm-10">@Model.DeliveryDate.ToString("dd.MM.yyyy HH:mm")</dd>
        </dl>

        <form asp-action="Delete" method="post">
            <input type="hidden" asp-for="Id" />
            <input type="submit" value="Видалити" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-secondary">Назад до списку</a>
        </form>
    </div>
</div>